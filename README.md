# Spilljournalen

Moderne norsk nettsted om Nintendo-spill.

## Lokalt

Åpne `index.html` i nettleseren, eller:

```bash
python3 -m http.server 8080
```

## Produksjon

GitHub Pages fra `main` (root). `CNAME` peker til `spilljournalen.no`.
